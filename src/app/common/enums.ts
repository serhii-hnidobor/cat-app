enum NavItemType {
  VOTING = 'VOTING',
  BREEDS = 'BREEDS',
  GALLERY = 'GALLERY'
}

enum IconNames {
  LOGO = "LOGO"
}

enum AppRoutes {
  VOTING = '/voting',
  BREEDS = '/breeds',
  GALLERY = '/gallery' 
}

export { NavItemType, IconNames, AppRoutes };